# Picking & world queries

> **Stub** — this chapter is planned for stage BK.5 (see
> [`PORTING-BOOK.md`](https://github.com/NCrashed/roxlap/blob/master/docs/porting/PORTING-BOOK.md)).

Will cover screen→world picking (`pick` / `view_ray` / `pick_depth`),
world queries (`Scene::raycast` / `resolve_voxel`), and the demo's
hand-rolled collision as the interim pattern until a dedicated
character-controller stage supersedes it.

Until then: the Picking demo scene, and
[docs.rs/roxlap-scene](https://docs.rs/roxlap-scene).
